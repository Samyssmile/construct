import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Design Principles" tags={['!autodocs']} />

<span className="ct-eyebrow">Foundations · Philosophy</span>

# Design Principles

Construct is built on three pillars: **accessibility first**, **token-based theming**, and **framework agnosticism**. Every decision — from API design to CSS architecture — flows from these principles.

---

## Accessibility First

Accessibility is not an afterthought or a checkbox. It is the primary constraint that shapes every component.

### What this means in practice

- **Semantic HTML by default** — components use `<button>`, `<input>`, `<table>`, `<nav>`, `<details>` instead of generic `<div>` elements with ARIA bolted on
- **Keyboard operable** — native elements work without a mouse; optional headless controllers implement the focus and keyboard state machines for composite widgets
- **Visible focus indicators** — all interactive elements show a clear `:focus-visible` treatment whose width and offset fit the component geometry
- **WCAG 2.2 AA target** — built-in themes pass the automated semantic/component contrast contract, with a dedicated high-contrast mode
- **State via ARIA attributes** — component states are expressed through `aria-expanded`, `aria-selected`, `aria-invalid`, and `aria-pressed` rather than visual-only CSS classes
- **`prefers-reduced-motion` respected** — animations and transitions are disabled when the user's system preference says so

### Why accessibility first?

Building accessible components from the start is dramatically cheaper than retrofitting. When accessibility drives the API, the resulting components are more robust, more testable, and usable by the widest possible audience — including users of screen readers, keyboard-only users, and users with low vision.

---

## Token-Based Theming

Reusable visual decisions in Construct — colors, spacing, typography, radii, shadows, and motion — are expressed as **design tokens**. Component-local geometry and structural constants stay with their owning component when they are not reusable system decisions.

### Three-layer pipeline

Construct uses three token layers:

1. **Primitives** — raw design values (`stone.950: #16130F`, `orange.500: #F4581C`, `space.4: 8px`). These are the palette. They rarely change.
2. **Semantic tokens** — purpose-driven, theme-aware aliases (`color.brand.primary → {color.stone.950}`).
3. **Component tokens** — stable component decisions (`button.height → {size.controlHeight.md}`) that reference semantic or primitive intent.

### Benefits

- **Consistency** — every component draws from the same set of values. No drift between a card's border radius and a modal's
- **Theming** — switching from light to dark mode is a single `data-theme` attribute change. Every component updates simultaneously via CSS custom properties
- **Customisation** — compile schema- and contrast-validated product overrides without copying generated CSS
- **Tooling** — tokens are available as CSS custom properties, JSON, JavaScript, and TypeScript exports. Use them at build time or runtime

### Rules

- Use token references (`var(--color-text-primary)`) for reusable or semantic design decisions; keep unavoidable structural constants local and documented
- Add new tokens to the source JSON and run `npm run build`. Never hand-edit generated files
- When in doubt, prefer an existing token over creating a new one

---

## Framework Agnostic

Construct is CSS-first and works with React, Angular, Svelte, Vue, Web Components, or plain HTML. Optional dependency-free ESM controllers add complex behavior without introducing a framework adapter or required runtime.

### How this works

- Components are activated by CSS classes (`ct-button`, `ct-card`, `ct-input`)
- State is driven by standard HTML attributes (`disabled`, `aria-expanded`, `data-state`)
- Composite behavior can use `@neuravision/construct/behaviors`; rendering and product data stay with the consumer

### Why optional JS?

- **Zero lock-in** — migrate from React to Svelte without changing your design system
- **Pay for behavior only when needed** — native/CSS-only patterns load no behavior module; ESM controllers tree-shake for composite widgets
- **Separation of concerns** — Construct CSS owns presentation, optional Construct controllers own documented interaction state machines, and the consumer owns rendering and product data

### Integration pattern

```html
<!-- Works everywhere -->
<button class="ct-button ct-button--secondary" type="button">Cancel</button>
```

```jsx
// React
<button className="ct-button ct-button--secondary" type="button">Cancel</button>
```

```svelte
<!-- Svelte -->
<button class="ct-button ct-button--secondary" type="button">Cancel</button>
```

The same HTML structure and class names work in every framework.

---

## Additional Principles

### Simplicity over cleverness

- Flat CSS selectors — no deep nesting, no specificity wars
- BEM naming — predictable, greppable, no collisions
- Three-tier sizing (sm, md, lg) — enough flexibility without decision fatigue

### Progressive enhancement

- Native patterns work without JavaScript: `<details>`, `<select>`, and `<button>` retain platform behavior
- Headless controllers enhance composite patterns with focus traps, roving tabindex, typeahead, dismissal, and cleanup

### Openness

- Source tokens are human-readable JSON
- Source tokens and generated standalone CSS entries are committed; generated token outputs (CSS, JSON, JavaScript, TypeScript) are built in CI and prepack and included in the package
- No proprietary toolchain — standard CSS, standard HTML, standard build scripts

---

## Related

- **[Installation](?path=/docs/get-started-installation--docs)** — installation and integration guide
- **[Accessibility](?path=/docs/foundations-accessibility--docs)** — detailed keyboard patterns and ARIA guidance
- **[Design Tokens](?path=/docs/foundations-design-tokens--docs)** — full token reference
- **[Component Patterns](?path=/docs/patterns-component-patterns--docs)** — BEM naming, state management, and ARIA patterns
